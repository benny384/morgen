import java.util.Scanner;

public class GetPassword {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Geben Sie den Satz ein: ");
        String input = scanner.nextLine();
        String[] words = input.split("\\s+");
        StringBuilder password = new StringBuilder();
        for (String word : words) {
            if (!word.isEmpty()) {
                char lastChar = Character.toLowerCase(word.charAt(word.length() - 1));
                if (Character.isLetter(lastChar)) {
                    password.append(Character.isUpperCase(lastChar) ? Character.toLowerCase(lastChar) : Character.toUpperCase(lastChar));
                } else {
                    password.append(lastChar);
                }
            }
        }
        System.out.println("Das Passwort ist: " + password.toString());
    }
}

